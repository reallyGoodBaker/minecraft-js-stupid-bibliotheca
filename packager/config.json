{
    "source": "${rootPath}/${targetFileName}",
    "target": "${distPath}/${targetFileName}.mcpack",
    "params": {
        "distPath": "${rootPath}/dist"
    },
    "autoImport": true,
    "needConfirm": false
}